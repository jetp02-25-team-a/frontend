'use client';

export default function MapPanel() {
  return (
    <aside className="w-1/3 min-h-screen bg-gray-100 border-l border-gray-200 flex items-center justify-center">
      <div className="text-gray-400">🗺️ 地圖區（尚未整合）</div>
    </aside>
  );
}
