'use client';

export default function MapPanel() {
  return (
    <aside className="w-1/3 min-h-screen bg-gray-100 border-l border-gray-200 flex flex-col items-center justify-center">
      <p className="text-gray-500 text-sm">🗺️ 這裡將顯示地圖或地點標記</p>
    </aside>
  );
}
