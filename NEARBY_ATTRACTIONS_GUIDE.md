# 🗺️ 附近景點功能使用說明

## 功能概述
您的行程編輯頁面現在已經集成了強大的**附近景點搜索功能**，可以根據當前地圖位置自動搜索並顯示周圍的景點，讓您更容易發現和添加新的旅遊景點到行程中。

## 🎯 主要功能

### 1. 附近景點搜索
- **智能範圍搜索**：根據地圖中心點搜索指定範圍內的景點
- **距離計算**：使用 Haversine 公式精確計算景點距離
- **景點分類**：支援觀光景點、自然景觀、歷史古蹟等分類
- **彈性半徑**：支援 1km、2km、5km、10km 等不同搜索範圍

### 2. 互動式地圖顯示
- **視覺化標記**：不同類型景點使用不同顏色標記
- **搜索範圍圓圈**：清楚顯示搜索範圍邊界
- **詳情視窗**：點擊標記顯示景點詳細資訊
- **響應式縮放**：自動調整地圖縮放層級

### 3. 側邊景點面板
- **景點列表**：顯示搜索到的所有附近景點
- **快速添加**：一鍵將景點加入當前選中的行程天數
- **詳細資訊**：顯示景點圖片、地址、距離等資訊
- **分類標籤**：清楚標示景點類型（觀光/自然/古蹟）

## 🚀 使用步驟

### 步驟 1：啟用附近景點模式
1. 在地圖區域找到右上角的控制面板
2. 勾選「附近景點」選項
3. 系統會自動切換到附近景點模式

### 步驟 2：設定搜索範圍
1. 在左側面板或控制面板中選擇搜索範圍
2. 可選擇：1公里、2公里、5公里、10公里
3. 地圖上會顯示藍色圓圈表示搜索範圍

### 步驟 3：瀏覽附近景點
1. 左側面板會顯示搜索到的所有景點
2. 地圖上會顯示彩色標記點
3. 點擊標記或列表項目查看詳細資訊

### 步驟 4：添加景點到行程
1. **先選擇天數**：在日期欄中選擇要加入的天數
2. **點擊加入按鈕**：在景點卡片上點擊「加入」按鈕
3. **確認添加**：景點會自動加入到選中的天數中

## 🎨 視覺指南

### 地圖標記顏色說明
- 🔴 **紅色**：觀光景點 (tourism)
- 🟢 **青色**：自然景觀 (natural)
- 🔵 **藍色**：歷史古蹟 (historic)
- 🟠 **橘色**：其他類型景點

### 景點面板資訊
- **景點圖片**：如有圖片會顯示縮圖
- **景點名稱**：中文名稱優先顯示
- **地址資訊**：完整地址資訊
- **距離顯示**：精確到小數點後兩位
- **類型標籤**：彩色標籤標示景點類型

## ⚙️ 技術特色

### 後端 API
```javascript
GET /api/itineraries/nearby?lat=25.033&lng=121.564&radius=2
```
- 使用 Haversine 公式計算距離
- 支援彈性搜索半徑
- 回傳排序好的景點列表（由近到遠）

### 前端組件
- **GoogleMapWithNearby**：增強版地圖組件
- **NearbyAttractionsPanel**：景點列表面板
- **即時更新**：地圖移動時自動重新搜索

### 狀態管理
- 附近景點資料快取
- 載入狀態管理
- 搜索範圍同步

## 🔧 自訂設定

### 修改預設值
```typescript
// 在 page.tsx 中修改
const [searchRadius, setSearchRadius] = useState(2); // 預設搜索範圍
const nodeData = {
  durationMinutes: 60, // 預設停留時間（分鐘）
};
```

### 添加新的搜索範圍
```jsx
<option value={15}>15 公里</option>
<option value={20}>20 公里</option>
```

## 🐛 注意事項

1. **選擇天數**：添加景點前必須先選擇要加入的天數
2. **API 限制**：避免過於頻繁的搜索請求
3. **網路連線**：需要穩定的網路連線才能載入景點資料
4. **地圖 API**：確保 Google Maps API 金鑰有效

## 📱 響應式設計

- **桌面版**：完整的側邊面板和地圖顯示
- **平板版**：自動調整面板寬度
- **手機版**：可收合的面板設計（待實現）

## 🎉 未來擴展

可以考慮的功能擴展：
- 景點評分和評論
- 自訂景點類型篩選
- 收藏景點功能
- 景點路線規劃
- 多語言支援

---

現在您可以在行程編輯頁面使用這個強大的附近景點功能了！🗺️✨